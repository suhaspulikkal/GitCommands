$ git status
$ git checkout text1.txt // To rundo the changes
$ git add .
$ git rm --cached -r . //to remove the files staged as added
$ git log

$ .gitignore //create this file and include the files that has to be exculded

when added or staged the files included in gitignore will not be shown

To push the local git repository to remote repository we can use the below code

$ git remote add origin https://github.com/suhaspulikkal/GitignoreConcept.git

$ git push -u origin master

$ git reset --soft HEAD~1 to remove the commits by 1 decriment

$ git remote 

$ git remote --v

$ git remote remove origin

$ rm -rf .git

$ rm -rf cloneFolderName

$ git branch alien-plot(the branch name) // to creata a branch

$ git branh // to see the list of branches 

$ git checkout branchName //to switch between branches

$ git merge branchName //to merge the alient plot to the main branch

$ :q! //to close the merge screen

